---
title: Интеграция с редакторами
description: "Интеграция ADP с редакторами: ссылки «Открыть в редакторе» для VS Code, PhpStorm, Cursor и других IDE. Маппинг путей для Docker/WSL/удалённых сред."
---

# Интеграция с редакторами

ADP отображает кнопку «Открыть в редакторе» рядом с каждой ссылкой на файл (исключения, логи, события, стек-трейсы, маршруты). Клик открывает вашу IDE на нужном файле и строке через пользовательский URL-протокол.

## Настройка

Откройте диалог настроек (меню в верхней панели → **Settings**) и выберите редактор в секции **Editor Integration**. Настройка хранится в `localStorage` браузера.

## Поддерживаемые редакторы

| Редактор | Формат URL |
|----------|-----------|
| PhpStorm | `phpstorm://open?file={file}&line={line}` |
| IntelliJ IDEA | `idea://open?file={file}&line={line}` |
| WebStorm | `webstorm://open?file={file}&line={line}` |
| GoLand | `goland://open?file={file}&line={line}` |
| PyCharm | `pycharm://open?file={file}&line={line}` |
| RubyMine | `rubymine://open?file={file}&line={line}` |
| Rider | `rider://open?file={file}&line={line}` |
| CLion | `clion://open?file={file}&line={line}` |
| VS Code | `vscode://file/{file}:{line}` |
| VS Code Insiders | `vscode-insiders://file/{file}:{line}` |
| Cursor | `cursor://file/{file}:{line}` |
| Sublime Text | `subl://open?url=file://{file}&line={line}` |
| Zed | `zed://file/{file}:{line}` |
| Custom | пользовательский шаблон с плейсхолдерами `{file}` / `{line}` |

## Маппинг путей (Docker / WSL / удалённые среды)

Когда приложение работает в контейнере или на удалённом хосте, пути к файлам, которые сообщаются ADP (например, `/app/src/Foo.php`), не совпадают с теми, что ожидает локальная IDE (например, `/Users/me/project/src/Foo.php`). Маппинг путей переписывает их перед сборкой URL редактора.

В диалоге настроек, под выбором редактора, добавьте строки **Remote → Local**:

| Remote | Local |
|--------|-------|
| `/app` | `/Users/me/project` |
| `/var/www` | `/home/user/site` |

Правила:

- Побеждает первый совпавший префикс.
- Пустые ключи Remote игнорируются.
- Дубликаты Remote подсвечиваются в UI; побеждает последнее значение.
- Маппинги сохраняются в `localStorage` вместе с пресетом редактора.

## Где появляются ссылки на редактор

| Компонент | Где |
|-----------|-----|
| `FileLink` | Любая одиночная ссылка на файл (логи, события, var dumps, исходник маршрута) |
| `StackTrace` | Каждый фрейм в стек-трейсе исключения |

Клик по пути открывает встроенный File Explorer ADP; клик по иконке редактора (рядом с путём) запускает вашу IDE.

## Запланировано

| Функция | Зачем |
|---------|-------|
| Транспорт через HTTP-колбэк | URL-протоколы работают не во всех браузерах/ОС; POST к локальному плагину редактора (например, PhpStorm `http://localhost:63342/api/file/...`) надёжнее |
| Поддержка source map | Стек-трейсы фронтенда сейчас указывают на собранные файлы — нужно резолвить обратно на исходники |
