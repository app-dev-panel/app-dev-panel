# Листенеры — когда декоратор не подходит

- состояние системы
- действие в середине класса
- сторонний сервис без интерфейса

→ подписываемся на pub/sub события фреймворка

<!--
Speaker notes:
Декораторы покрывают всё, что описано интерфейсом. Но
бывают штуки, которые обернуть просто нечем: глобальное
состояние, действие внутри метода без точек входа,
сторонний сервис без PSR-контракта.

Там в дело вступают event listeners. Мы слушаем события
фреймворка — kernel.request, mail.sent, cache.hit и так
далее — и пишем в коллекторы: что пришло, когда, откуда.
Это дополняет декораторы, а не конкурирует с ними.
-->
