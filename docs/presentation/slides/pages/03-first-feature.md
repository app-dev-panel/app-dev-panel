# Первая фича: повтор запроса + cURL

- кнопка «повторить» прямо в панели
- «скопировать как cURL» — для запросов, улетевших из памяти

<!--
Speaker notes:
Сделал кнопку «повторить запрос» и сразу понял, что этого
мало. Часть запросов улетает из памяти дебаггера быстрее,
чем я успеваю их воспроизвести — повторить их уже нечем.

Поэтому рядом появилась вторая кнопка — «скопировать как
cURL». Один клик — и запрос живёт дольше, чем сессия
дебаггера: можно сохранить в заметку, вставить в любой
клиент, повторить через день.

Это типичная для всей панели история: одна идея тянет
другую. С этого момента начался осознанный подход —
собирать данные так, чтобы их можно было переиспользовать.
-->
