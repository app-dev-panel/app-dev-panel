# MCP-сервер внутри ADP

- список debug-записей
- поиск по значениям
- выгрузка состояния объектов

ИИ дебажит через ADP, а не просит «собери руками».

<!--
Speaker notes:
Раз везде ИИ — ему нужен стандартный интерфейс к нашим
данным. Так внутри ADP появился MCP-сервер.

Что он сейчас умеет: отдавать список debug-записей,
искать значения по строковому запросу, выгружать
состояние объектов. То есть всё, что агент раньше
просил «сделать руками», теперь он делает сам через
MCP-вызов.

Пока оптимизация не самая крутая — формат JSON,
там много лишних байт. Но главное уже работает: ИИ
дебажит ЧЕРЕЗ ADP, а не вокруг него.
-->
