# Декораторы PSR-интерфейсов

```php
final class LoggerInterfaceProxy implements LoggerInterface
{
    public function log($level, string $message, array $context = []): void
    {
        $this->collector->record($level, $message, $context);

        $this->original->log($level, $message, $context);
    }
}
```

- пишем в коллектор, потом делегируем оригиналу
- пользовательский код ни о чём не знает

<!--
Speaker notes:
PSR-интерфейсы дают прямую точку врезки. Любой класс,
реализующий PSR-контракт, можно обернуть прокси-адаптером
за пять минут.

Адаптер внутри делает две вещи: сначала пишет данные в
наш коллектор, потом передаёт вызов дальше в настоящий
сервис. Можно наоборот — сначала делегировать, потом
записать результат. Можно обе — вход и выход.

Главное — снаружи ничего не меняется. Код, который
зовёт $logger->info(), не знает и не должен знать,
что логгер уже завёрнут.
-->
