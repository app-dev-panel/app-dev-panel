# Всё есть — но ИИ за бортом

- данные собираем
- фронтенд показывает
- инспектор управляет состоянием
- а с ИИ: «сходи в папку, посмотри логи»

Делаем шаг конём: MCP внутри ADP + свой ИИ-клиент.
OpenRouter — без платного API. Или Claude с подпиской.

<!--
Speaker notes:
К этому моменту в ADP уже есть всё: коллекторы собирают
данные за каждый реквест, фронтенд их отображает,
инспектор позволяет управлять состоянием и дёргать
команды прямо из панели.

И вот тут приходит ИИ. И всё что он умеет — попросить
тебя сходить в папку и прочитать логи. Всё что мы
построили — мимо.

Я сделал шаг конём. Во-первых, MCP-сервер прямо внутри
ADP — чтобы агент мог дёргать наши данные напрямую,
без логов. Во-вторых, свой ИИ-клиент в панели — чтобы
не прыгать между вкладками.

Чтобы не платить за API — подключил OpenRouter. Или
можно использовать Claude с подпиской. Всё для вас.
-->
