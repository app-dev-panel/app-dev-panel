# Попробовать за две минуты

```bash
composer require app-dev-panel/adapter-symfony
# или adapter-laravel, adapter-yii3, adapter-yii2
```

или скорми `llms.txt` своей нейронке — она сама поставит

<!--
Speaker notes:
Два пути.

Первый — классический: composer require нужного адаптера,
он сам подтянет ядро и API. Один пакет на фреймворк,
без лишних зависимостей.

Второй — ленивый и мой любимый. У ADP есть llms.txt —
машиночитаемая документация для LLM-агентов. Открываешь
ChatGPT или Claude, скармливаешь llms.txt, пишешь
«установи мне это» — и агент сам читает доки и
прописывает всё в твой проект. Буквально за один промпт.

И это тот самый случай, где ИИ помогает поставить
инструмент, который потом поможет ИИ дебажить.
-->
