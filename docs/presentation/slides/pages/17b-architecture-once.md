# Фичу пишешь один раз

Я не заявляю **честный** фича-паритет.

<v-click>

<div class="mt-10 text-lg">
Но архитектура сделана так, что поддержать его — <b>дёшево</b>.<br/><br/>
Пишу одну фичу в <b>ядре</b> — и все адаптеры получают обновление <b>плюс-минус автоматически</b>.
</div>

</v-click>

<!--
Speaker notes:
Это честная версия «фича-паритета». Не обещаем, что всё
одинаково прямо сейчас — но объясняем, почему дешево
держать несколько адаптеров в синхроне. Ядро отделено,
адаптеры тонкие, поэтому одна правка ядра расходится
на всех по умолчанию.
-->
