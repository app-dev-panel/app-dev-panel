# Работает со всем, что имеет PSR-лицо

- логгер
- DI-контейнер
- event dispatcher
- HTTP-клиент
- кэш

Пользовательский код об этом не знает.

<!--
Speaker notes:
Всё, что описано PSR-интерфейсом, можно обернуть
одинаковым способом — это главное преимущество общих
стандартов. PSR-3 логгер, PSR-11 контейнер, PSR-14
event dispatcher, PSR-18 HTTP-клиент, PSR-16 кэш.

Везде одна и та же схема: берём сервис из DI, заменяем
на прокси, прокси складывает данные в коллектор.
Пользовательский код абсолютно не в курсе. Не меняется
ни одна строчка в контроллерах, моделях, сервисах.

Вот это «пользовательский код не знает» — и есть вся
ценность архитектурного подхода. Дальше будет слой
листенеров, но фундамент — здесь.
-->
