import * as Pages from '@app-dev-panel/panel/Module/GenCode/Pages';
import {RouteObject} from 'react-router';

export const routes = [{path: '/gen-code', element: <Pages.Layout />}] satisfies RouteObject[];
